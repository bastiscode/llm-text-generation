experiment:
  name: phi-2

model:
  model: phi-2
  type: pretrained_decoder

seed: 22

inference:
  max_length: 2048
  window:
    type: full
  tokenizer: file(../../configs/tokenizers/phi-2.yaml)
  eos: <|endoftext|>
